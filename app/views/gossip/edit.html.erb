
  <% if @up.errors.any? %><div class="alert alert-danger" role="alert">
  <p>Nous n&rsquo;avons pas réussi à modifier le potin pour la (ou les) raison(s) suivante(s) :
  <ul>
    <% @up.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>  
  </ul></p></div>

<% end %>

<h1 class="text-center">Modifier le potins</h1>
</br>
<div class="container w-50">

<%= form_tag url_for(action: 'update'), method: "patch" do %>
  <div class="form-group">
  <%= label_tag 'Title' %>
  <%= text_field_tag 'title', @up.title, class: 'form-control' %>
  </div>
  <div class="form-group">
  <%= label_tag 'Body' %>
  <%= text_area_tag 'content', @up.content, class: 'form-control' %>
  </div>
  <div class="form-group">
  <%= submit_tag 'Update Gossip', class: 'btn btn-primary' %>
  </div>
<% end %>

</div>