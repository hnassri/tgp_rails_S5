<div class="m-2">
  <h1 class="text-center">Bienvenue sur la page de la ville de <%=  @city_id.name%></h1>
  </br>
    <div class="container">
    </br>
    <h3 class="text-center">Voici la listes des potins:</h3>
    </br>
    <div class="row justify-content-around">
    <% @city_id.users.all.each do |user| %>
      <% user.gossips.all.each do |gossip| %>
      <div class="col-sm-3 mb-4">
        <div class="card">
          <div class="card-body">
            <h5 class="card-title text-center"><%=    gossip.user.first_name %></h5>
            <p class="card-text text-center"><%= link_to gossip.title, gossip_path(gossip.id)%></a></p>
            
          </div>
        </div>
      </div>
    <% end%>
    <% end %>
      </div>
  </div>
   